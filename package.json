{
  "name": "srtos-qld-a3-mysql-api",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "description": "Assessment 3 MySQL account/profile/saved-route/alerts/notification API for SRTOS QLD",
  "scripts": {
    "start": "node --env-file=.env server.mjs",
    "dev": "node --env-file=.env --watch server.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check server.mjs && node --check src/app.mjs && node --check src/mysql-repository.mjs && node --check src/security.mjs",
    "smoke:mysql": "node --env-file=.env scripts/mysql-smoke.mjs",
    "check:schema": "node scripts/check-schema.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "mysql2": "^3.14.5"
  }
}
